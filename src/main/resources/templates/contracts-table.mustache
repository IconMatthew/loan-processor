<!DOCTYPE HTML>
<html>
<head>
    <title>Contracts</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <link rel="stylesheet" type="text/css" href="../static/css/style.css">
</head>
<body>

<style>
    body{
        font-family: 'Montserrat', sans-serif;
        color: darkslateblue;
        font-weight: 500;
    }

    .col4{
        height: auto;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        border-radius: 20px;
        border: 1px solid darkslateblue;
        background-color: cornflowerblue;
    }

    input[type="text"], input[type="number"]{
        height: 30px;
        width: 200px;
        font-size: 14px;
        border-radius: 10px;
        border: 1px solid skyblue;
        padding: 2px 10px;
        color: black;
    }

    .wrapper{
        width: 1200px;
        margin: auto;
        margin-top: 10%
    }

    h1{
        font-size: 25px;
        color: white;
    }

    input {
        outline: none;
        border: 1px solid black;
    }

    input:focus{
        border: 1px solid blue;
    }

    .listElem{
        display: flex;
        flex-direction: column
    }

    label{
        margin-bottom: 5px;
        margin-left: 5px;
    }

    .listElemLast button{
        height: 30px;
        width: 200px;
        font-size: 14px;
        border-radius: 10px;
        border: 1px solid white;
        color: white;
        background-color: darkslateblue;
    }

    a{
        line-height: 35px;
        font-weight: 500;
        color: black;
        text-decoration: none;
        font-size: 15px;
    }

    a:hover{
        color: cornflowerblue;
    }

    .linker a{
        padding: 15px;
    }

    .linker{
        margin-top: 20px;
        background-color: white;
        border-radius: 20px;
        margin-bottom: 20px;
    }
    table {
        font-family: "Lucida Sans Unicode", "Lucida Grande", Sans-Serif;
        font-size: 14px;
        border-collapse: collapse;
        text-align: center;
    }
    th, td:first-child {
        background: darkslateblue;
        color: white;
        padding: 10px 20px;
    }
    th, td {
        border-style: solid;
        border-width: 0 1px 1px 0;
        border-color: white;
    }
    td {
        background: #D8E6F3;
    }
    th:first-child, td:first-child {
        text-align: left;
    }
    .filterValue{
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 3px;
    }
    .filterValue label{
        margin-right: 20px;
        color: white

    }
    button{
        height: 30px;
        width: 200px;
        font-size: 14px;
        border-radius: 10px;
        color: white;
        background-color: darkslateblue;
    }
    button:hover{
        opacity: 0.9;
    }

    .buttonCont{
        margin-top: 10px;
        display: flex;
        justify-content: center;
    }

    .buttonCont1{
        margin-top: 5px;
        margin-bottom: 10px;
        display: flex;
        justify-content: center;
    }

</style>


<div class="wrapper">
    <div class="col4">

        <h1>Договоры</h1>

        <div>
            <table>
                <tr>
                    <th>ФИО клиента</th>
                    <th>Сумма (рубли)</th>
                    <th>Срок (дни)</th>
                    <th>Статус подписания</th>
                    <th>Дата подписания</th>
                </tr>
                {{#contracts}}
                    <tr>
                        <td>{{loanApplication.client.fullName}}</td>
                        <td>{{loanApplication.approvedAmount}}</td>
                        <td>{{loanApplication.term}}</td>
                        <td>
                            {{#signatureStatus}}
                                Подписан
                            {{/signatureStatus}}
                            {{^signatureStatus}}
                                Не подписан
                            {{/signatureStatus}}
                        </td>
                        <td>{{signatureDate}}</td>
                    </tr>
                {{/contracts}}
            </table>
        </div>

        <div class="linker">
            <a class="backToMain" href="/client/main-page">На главную</a>
        </div>
    </div>
</div>

</body>
</html>